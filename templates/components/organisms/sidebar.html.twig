<aside class="sidebar">
    <div class="sidebar__inner">
        <nav class="nav">
            <div class="nav-logo">
                {% include 'components/atoms/logo.html.twig' %}
            </div>
        </nav>
        
        <div class="hero-mini">
            <span class="hero-eyebrow">{{ hero_eyebrow|default('JEAN-MICHEL TROUVETOU'|trans) }}</span>
            <h1 class="hero-title">{{ hero_title|default('Trouvez votre <strong>talent.</strong>'|trans)|raw }}</h1>
        </div>

        <div class="search-panel">
            {% block sidebar_content %}{% endblock %}
        </div>

        <div class="lang-switcher">
            <a href="{{ path('change_locale', {'locale': 'fr'}) }}" class="lang-link {% if app.request.locale == 'fr' %}active{% endif %}">FR</a>
            <span class="lang-sep">|</span>
            <a href="{{ path('change_locale', {'locale': 'en'}) }}" class="lang-link {% if app.request.locale == 'en' %}active{% endif %}">EN</a>
        </div>

        {% include 'components/molecules/sidebar_footer.html.twig' %}
    </div>
</aside>